let state = 0;

function getState() {
    return state
}

const actions = {type : 'INCREMENT',
    type : 'DECREMENT',
    type : 'RESET'
}

console.log(getState())

function tallyApp() {
    switch (action.state) {
        case ""
    }
}